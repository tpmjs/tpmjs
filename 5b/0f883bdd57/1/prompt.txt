Implement the following plan:

# Plan: Comprehensive Metrics, Usage & Event Tracking

## Context

The audit identified 18 gaps in tracking coverage. Vercel Analytics handles page-level metrics (views, referrers, sessions) for all pages. Custom Prisma-based tracking exists for executions, API usage, page views, and daily snapshots. But several areas lack tracking, cleanup crons aren't wired, the stats snapshot is incomplete, and there's no admin dashboard to surface it all.

No new analytics providers — leverage existing Vercel Analytics + custom Prisma tracking.

## Batch 1: Schema Migration

Add new models and fields to `packages/db/prisma/schema.prisma`:

1. **`SearchLog` model** — track search queries, result counts, latency
   - Fields: `id`, `query`, `resultCount`, `latencyMs`, `userId?`, `sessionId?`, `createdAt`
   - Index on `createdAt` for cleanup

2. **`UserRole` enum** — `USER`, `ADMIN`
   - Add `role UserRole @default(USER)` field to `User` model

3. **`StatsSnapshot` new fields** — `dauCount`, `wauCount`, `mauCount`, `searchCount`, `avgSearchLatencyMs`, `topSearchQueries` (Json), `mcpUniqueClients`

4. **`User.signupSource`** — optional String field to track where users came from (e.g., `'github'`, `'google'`, `'direct'`)

Then run `pnpm --filter=@tpmjs/db db:generate` and `pnpm --filter=@tpmjs/db db:push`.

### Files
| Action | Path |
|--------|------|
| Edit | `packages/db/prisma/schema.prisma` |

---

## Batch 2: Search Tracking + Cleanup Crons

### 2a. Search tracking library
Create `apps/web/src/lib/tracking/search.ts`:
- `trackSearch(params)` — fire-and-forget `prisma.searchLog.create()`
- `cleanupOldSearchLogs(daysToKeep = 90)` — delete old records
- Follow pattern from `apps/web/src/lib/tracking/executions.ts`

### 2b. Wire search tracking into search endpoint
Edit `apps/web/src/app/api/tools/search/route.ts`:
- Import `trackSearch` and call it before returning results (line ~227)
- Pass `query`, `resultCount`, `latencyMs` (measure with `performance.now()`)

### 2c. Cleanup crons (3 new routes)
Each follows the pattern in `apps/web/src/app/api/sync/cleanup-activity/route.ts` (auth check, try/catch, SyncLog, SyncCheckpoint):

1. `apps/web/src/app/api/sync/cleanup-executions/route.ts` — calls existing `cleanupOldExecutionEvents()` from `src/lib/tracking/executions.ts`
2. `apps/web/src/app/api/sync/cleanup-api-usage/route.ts` — calls existing `cleanupOldUsageRecords()` from `src/lib/api-keys/usage.ts`
3. `apps/web/src/app/api/sync/cleanup-search-logs/route.ts` — calls `cleanupOldSearchLogs()` from new search tracking lib

### 2d. Register crons in vercel.json
Add 3 entries running daily at staggered times (3:15, 3:30, 3:45 UTC).

### Files
| Action | Path |
|--------|------|
| Create | `apps/web/src/lib/tracking/search.ts` |
| Edit | `apps/web/src/app/api/tools/search/route.ts` |
| Create | `apps/web/src/app/api/sync/cleanup-executions/route.ts` |
| Create | `apps/web/src/app/api/sync/cleanup-api-usage/route.ts` |
| Create | `apps/web/src/app/api/sync/cleanup-search-logs/route.ts` |
| Edit | `vercel.json` |

---

## Batch 3: Stats Snapshot Enhancements

Edit `apps/web/src/app/api/sync/stats-snapshot/route.ts` to add new queries to the existing `Promise.all`:

- **DAU/WAU/MAU**: Count distinct `userId` from `UserActivity` for 1d/7d/30d windows
- **Search volume**: Count from `SearchLog` for last 24h, avg latency, top 10 queries by frequency
- **MCP unique clients**: Count distinct `sessionId` or `userId` from `ExecutionEvent` where source = 'mcp' in last 7d

Store in the new `StatsSnapshot` fields added in Batch 1.

### Files
| Action | Path |
|--------|------|
| Edit | `apps/web/src/app/api/sync/stats-snapshot/route.ts` |

---

## Batch 4: Admin Dashboard

### 4a. Admin auth helper
Create `apps/web/src/lib/auth/admin.ts`:
- `isAdmin(userId: string): Promise<boolean>` — checks `user.role === 'ADMIN'`
- `requireAdmin(request): Promise<Session>` — throws 403 if not admin

### 4b. Admin API routes
1. `apps/web/src/app/api/admin/stats/route.ts` — returns latest StatsSnapshot + trend data (last 30 snapshots)
2. `apps/web/src/app/api/admin/users/route.ts` — paginated user list with activity counts, tier, role
3. `apps/web/src/app/api/admin/search-logs/route.ts` — recent searches, top queries, search volume chart data

### 4c. Admin dashboard pages
1. `apps/web/src/app/dashboard/admin/page.tsx` — overview with key metrics cards + charts (reuse existing d3 chart components from `src/components/stats/`)
2. `apps/web/src/app/dashboard/admin/users/page.tsx` — user management table

### 4d. DashboardLayout sidebar
Edit `apps/web/src/components/dashboard/DashboardLayout.tsx`:
- Add conditional "Admin" nav section (only shown when `session.user.role === 'ADMIN'`)
- Links: `/dashboard/admin` (Overview), `/dashboard/admin/users` (Users)

### Files
| Action | Path |
|--------|------|
| Create | `apps/web/src/lib/auth/admin.ts` |
| Create | `apps/web/src/app/api/admin/stats/route.ts` |
| Create | `apps/web/src/app/api/admin/users/route.ts` |
| Create | `apps/web/src/app/api/admin/search-logs/route.ts` |
| Create | `apps/web/src/app/dashboard/admin/page.tsx` |
| Create | `apps/web/src/app/dashboard/admin/users/page.tsx` |
| Edit | `apps/web/src/components/dashboard/DashboardLayout.tsx` |

---

## Batch 5: Public Stats & Conversion Tracking

### 5a. Signup source tracking
Edit auth config to capture `signupSource` from query params/referrer during registration. Edit `apps/web/src/lib/auth/auth.ts` to pass source through to user creation.

### 5b. Public activity feed enhancement
Edit `apps/web/src/app/api/activity/route.ts` (if exists) to include richer event types (tool additions, version updates, popular searches).

### Files
| Action | Path |
|--------|------|
| Edit | `apps/web/src/lib/auth/auth.ts` |
| Edit | `apps/web/src/app/api/activity/route.ts` (if exists) |

---

## Batch 6: Documentation

Update `CLAUDE.md` with:
- New cron endpoints and their schedules
- Admin dashboard routes
- How to promote a user to admin (`UPDATE users SET role = 'ADMIN' WHERE email = '...'`)

### Files
| Action | Path |
|--------|------|
| Edit | `CLAUDE.md` |

---

## Verification

```bash
# After schema changes
pnpm --filter=@tpmjs/db db:generate
pnpm --filter=@tpmjs/db db:push

# Type-check and build
pnpm type-check
pnpm build

# Test search tracking manually
curl -s "https://localhost:3000/api/tools/search?q=test" | jq .

# Verify admin routes (after promoting self to admin)
# UPDATE users SET role = 'ADMIN' WHERE email = 'your@email.com';
curl -s localhost:3000/api/admin/stats -H "Cookie: ..." | jq .
```

## Summary

- **New models**: SearchLog, UserRole enum
- **New fields**: StatsSnapshot (DAU/WAU/MAU, search metrics, MCP clients), User (role, signupSource)
- **New crons**: 3 cleanup crons (executions, API usage, search logs)
- **New tracking**: Search query logging with latency
- **New pages**: Admin dashboard (overview + users)
- **New API routes**: 3 admin routes + search tracking
- **Modified**: Stats snapshot cron, search endpoint, DashboardLayout, auth config, CLAUDE.md


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/ajaxdavis/.REDACTED.jsonl

---

This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation.

Analysis:
Let me carefully analyze the entire conversation chronologically:

1. The user provided a detailed plan for "Comprehensive Metrics, Usage & Event Tracking" with 6 batches.

2. I created task tracking items for all 6 batches.

3. **Batch 1: Schema Migration**
   - Read the full Prisma schema at `packages/db/prisma/schema.prisma`
   - Added `UserRole` enum (USER, ADMIN)
   - Added `role` and `signupSource` fields to User model
   - Added new fields to StatsSnapshot: dauCount, wauCount, mauCount, searchCount, avgSearchLatencyMs, topSearchQueries, mcpUniqueClients
   - Added SearchLog model with id, query, resultCount, latencyMs, userId, sessionId, createdAt
   - Ran `pnpm --filter=@tpmjs/db db:generate` successfully
   - Had issues with `db:push` due to missing env vars - fixed by using `set -a && source .env.local && set +a`

4. **Batch 2: Search Tracking + Cleanup Crons**
   - Created `apps/web/src/lib/tracking/search.ts` (trackSearch, cleanupOldSearchLogs)
   - Modified `apps/web/src/app/api/tools/search/route.ts` to add search tracking
   - Created 3 cleanup cron routes following the cleanup-activity pattern
   - Updated vercel.json with 3 new cron entries

5. **Batch 3: Stats Snapshot Enhancements**
   - Modified stats-snapshot route to add DAU/WAU/MAU queries, search metrics, MCP unique clients
   - Added new destructured results and stored them in snapshot creation

6. **Batch 4: Admin Dashboard**
   - Created `apps/web/src/lib/admin.ts` with isAdmin and requireAdmin helpers
   - Created 3 admin API routes: stats, users, search-logs
   - Created 2 admin dashboard pages: overview and users
   - Modified DashboardLayout to add admin nav section conditionally

7. **Batch 5: Signup Source & Activity Feed**
   - Modified auth.ts to add databaseHooks for tracking signup source
   - Enhanced public activity feed to include more event types

8. **Batch 6: Documentation**
   - Updated CLAUDE.md with new cron endpoints and admin dashboard info

9. **Verification**
   - Ran `pnpm type-check` which triggered a full build + type-check
   - All 422 tasks passed successfully with exit code 0

The user's only messages were:
1. The initial plan implementation request
2. This summary request

Let me now ensure I capture all file changes, code patterns, and technical decisions accurately.

Summary:
1. Primary Request and Intent:
   The user asked me to implement a comprehensive plan for "Comprehensive Metrics, Usage & Event Tracking" across 6 batches. The plan was fully detailed with specific files to create/edit, models to add, and patterns to follow. The work covered: Prisma schema migrations, search query tracking, cleanup crons, stats snapshot enhancements, an admin dashboard with auth, signup source tracking, activity feed enhancements, and documentation updates.

2. Key Technical Concepts:
   - Turborepo monorepo with pnpm workspaces
   - Next.js 16 App Router (`apps/web`)
   - PostgreSQL via Prisma (`packages/db`)
   - Better-auth for authentication (with `prismaAdapter`, `databaseHooks`)
   - Fire-and-forget tracking pattern (`.catch()` on prisma creates, never throwing)
   - Cron job pattern: auth check with CRON_SECRET, try/catch, SyncLog + SyncCheckpoint
   - Admin role-based access control via Prisma UserRole enum
   - `@tpmjs/ui` component library (Button, Badge, Icon, Input, Spinner, Select)
   - D3-based chart components (AreaChart) in `src/components/stats/`
   - `useSession()` from `@/lib/auth-client` for client-side auth
   - Raw SQL queries via `prisma.$queryRaw` for aggregation
   - `take: limit + 1` pagination pattern (no `count()`)

3. Files and Code Sections:

   - **`packages/db/prisma/schema.prisma`** (Edited)
     - Core schema file for all database models
     - Added `UserRole` enum (USER, ADMIN)
     - Added `role UserRole @default(USER)` and `signupSource String?` to User model
     - Added 7 new fields to StatsSnapshot: dauCount, wauCount, mauCount, searchCount, avgSearchLatencyMs, topSearchQueries, mcpUniqueClients
     - Added new `SearchLog` model at end of file:
     ```prisma
     model SearchLog {
       id          String   @id @default(cuid())
       query       String   @db.VarChar(500)
       resultCount Int      @map("result_count")
       latencyMs   Int      @map("latency_ms")
       userId      String?  @map("user_id")
       sessionId   String?  @map("session_id")
       createdAt   DateTime @default(now()) @map("created_at")
       @@index([createdAt])
       @@index([query])
       @@map("search_logs")
     }
     ```

   - **`apps/web/src/lib/tracking/search.ts`** (Created)
     - New fire-and-forget search tracking library following pattern from `executions.ts`
     - Exports `trackSearch(params)` and `cleanupOldSearchLogs(daysToKeep = 90)`
     ```typescript
     import { prisma } from '@tpmjs/db';
     export interface TrackSearchParams {
       query: string;
       resultCount: number;
       latencyMs: number;
       userId?: string;
       sessionId?: string;
     }
     export function trackSearch(params: TrackSearchParams): void {
       prisma.searchLog
         .create({
           data: {
             query: params.query.substring(0, 500),
             resultCount: params.resultCount,
             latencyMs: params.latencyMs,
             userId: params.userId,
             sessionId: params.sessionId,
           },
         })
         .catch((error) => {
           console.error('[trackSearch] Failed to record search event:', error);
         });
     }
     export async function cleanupOldSearchLogs(daysToKeep = 90): Promise<number> {
       const cutoff = new Date();
       cutoff.setDate(cutoff.getDate() - daysToKeep);
       const result = await prisma.searchLog.deleteMany({
         where: { createdAt: { lt: cutoff } },
       });
       return result.count;
     }
     ```

   - **`apps/web/src/app/api/tools/search/route.ts`** (Edited)
     - Added `import { trackSearch } from '~/lib/tracking/search';`
     - Added `const searchStart = performance.now();` before the try block
     - Added trackSearch call before returning results:
     ```typescript
     if (query) {
       const latencyMs = Math.round(performance.now() - searchStart);
       trackSearch({
         query,
         resultCount: results.length,
         latencyMs,
       });
     }
     ```

   - **`apps/web/src/app/api/sync/cleanup-executions/route.ts`** (Created)
     - Cleanup cron for execution events older than 90 days
     - Calls `cleanupOldExecutionEvents(90)` from `~/lib/tracking/executions`
     - Follows cleanup-activity pattern with auth, SyncLog, SyncCheckpoint

   - **`apps/web/src/app/api/sync/cleanup-api-usage/route.ts`** (Created)
     - Cleanup cron for API usage records older than 30 days
     - Calls `cleanupOldUsageRecords(30)` from `~/lib/api-keys/usage`

   - **`apps/web/src/app/api/sync/cleanup-search-logs/route.ts`** (Created)
     - Cleanup cron for search logs older than 90 days
     - Calls `cleanupOldSearchLogs(90)` from `~/lib/tracking/search`

   - **`vercel.json`** (Edited)
     - Added 3 new cron entries after cleanup-activity:
     ```json
     { "path": "/api/sync/cleanup-executions", "schedule": "15 3 * * *" },
     { "path": "/api/sync/cleanup-api-usage", "schedule": "30 3 * * *" },
     { "path": "/api/sync/cleanup-search-logs", "schedule": "45 3 * * *" }
     ```

   - **`apps/web/src/app/api/sync/stats-snapshot/route.ts`** (Edited)
     - Added 8 new parallel queries to the existing Promise.all: DAU/WAU/MAU (groupBy userId from UserActivity for 1d/7d/30d), search count, avg search latency, top 10 search queries (raw SQL), MCP unique clients (raw SQL)
     - Added corresponding destructured results: dauResult, wauResult, mauResult, searchCountResult, avgSearchLatencyResult, topSearchQueriesResult, mcpUniqueClientsResult
     - Added new fields to snapshot creation data

   - **`apps/web/src/lib/admin.ts`** (Created)
     - Admin auth helper with `isAdmin(userId)` and `requireAdmin()` functions
     - `requireAdmin()` throws Response objects (401/403) for use in API routes
     ```typescript
     export async function requireAdmin() {
       const session = await auth.api.getSession({ headers: await headers() });
       if (!session) {
         throw new Response(JSON.stringify({ error: 'Unauthorized' }), { status: 401, headers: { 'Content-Type': 'application/json' } });
       }
       const admin = await isAdmin(session.user.id);
       if (!admin) {
         throw new Response(JSON.stringify({ error: 'Forbidden' }), { status: 403, headers: { 'Content-Type': 'application/json' } });
       }
       return session;
     }
     ```

   - **`apps/web/src/app/api/admin/stats/route.ts`** (Created)
     - GET endpoint returning latest StatsSnapshot + 30 trend snapshots + totalUsers + totalSessions
     - Serializes BigInt fields to Number for JSON

   - **`apps/web/src/app/api/admin/users/route.ts`** (Created)
     - GET endpoint with pagination, search, sorting
     - Returns users with _count of collections, agents, activities, toolLikes, apiKeys, tpmjsApiKeys
     - Uses `take: limit + 1` for hasMore pagination

   - **`apps/web/src/app/api/admin/search-logs/route.ts`** (Created)
     - GET endpoint returning recentSearches (last 50), topQueries (raw SQL groupBy), dailyVolume (raw SQL date grouping)

   - **`apps/web/src/app/dashboard/admin/page.tsx`** (Created)
     - Client component admin overview page with StatCard components
     - Sections: Key Metrics (users, DAU/WAU/MAU), Registry, Activity, Health
     - AreaChart trend charts for tools, active users, executions, search volume
     - Top search queries table
     - Uses DashboardLayout, fetches from /api/admin/stats

   - **`apps/web/src/app/dashboard/admin/users/page.tsx`** (Created)
     - Client component user management table with search, pagination
     - Displays user info, role badges, tier, signup source, counts, join date
     - Uses DashboardLayout with showBackButton

   - **`apps/web/src/components/dashboard/DashboardLayout.tsx`** (Edited)
     - Added `adminNavItems` array with Overview and Users links
     - Added `isAdminUser` state with useEffect fetching /api/admin/stats to check access
     - Added conditional "Admin" nav section in sidebar after Likes section

   - **`apps/web/src/lib/auth.ts`** (Edited)
     - Added `databaseHooks.user.create.after` to track signup source
     - Looks up the account's providerId and sets it as signupSource on the user

   - **`apps/web/src/app/api/activity/public/route.ts`** (Edited)
     - Added TOOL_EXECUTED, COLLECTION_TOOLS_BULK_ADDED, AGENT_CONVERSATION_STARTED to PUBLIC_ACTIVITY_TYPES
     - Updated mapActivityType to handle new types

   - **`CLAUDE.md`** (Edited)
     - Added 3 new cleanup cron endpoints to the manual triggers section
     - Added Admin Dashboard section with API routes and SQL to promote user to admin

   - **Reference files read but not modified:**
     - `apps/web/src/lib/tracking/executions.ts` - fire-and-forget pattern reference
     - `apps/web/src/app/api/sync/cleanup-activity/route.ts` - cleanup cron pattern reference
     - `apps/web/src/lib/api-keys/usage.ts` - has `cleanupOldUsageRecords()` function
     - `apps/web/src/env.ts` - env validation schema
     - `apps/web/src/app/dashboard/activity/page.tsx` - dashboard page pattern reference
     - `apps/web/src/components/stats/AreaChart.tsx` - chart component interface reference
     - `apps/web/src/lib/auth-client.ts` - client-side auth exports

4. Errors and fixes:
   - **`pnpm --filter=@tpmjs/db db:push` failed with "Environment variable not found: DATABASE_URL_UNPOOLED"**
     - First tried `source .env.local && pnpm --filter=@tpmjs/db db:push` - still failed because sourcing doesn't export
     - Then tried `cd packages/db && DATABASE_URL_UNPOOLED=... npx prisma db push` - failed because DATABASE_URL also not set
     - Fixed with `set -a && source .env.local && set +a && pnpm --filter=@tpmjs/db db:push` which exports all vars from .env.local
     - Root cause: Prisma in packages/db/ doesn't have its own .env file; env vars must be exported from root .env.local

5. Problem Solving:
   - All 6 batches implemented successfully
   - Full build + type-check passed (422 tasks, 0 failures, exit code 0)
   - The BetterAuthError warnings during build about "default secret" are expected/pre-existing (env vars not available during static generation)
   - Admin check in DashboardLayout uses a fetch to /api/admin/stats as a lightweight way to verify admin access without needing to extend the better-auth session type

6. All user messages:
   - Message 1: "Implement the following plan: # Plan: Comprehensive Metrics, Usage & Event Tracking" - Full detailed plan with 6 batches, file lists, and implementation instructions. Referenced a plan transcript file.
   - Message 2: (This summary request)

7. Pending Tasks:
   - None. All 6 batches were completed and verified with a successful build + type-check.

8. Current Work:
   All 6 batches of the plan have been fully implemented and verified. The last action was running `pnpm type-check` which triggered a full monorepo build and type-check, completing successfully with exit code 0. All new routes appear in the Next.js build output (api/admin/stats, api/admin/users, api/admin/search-logs, api/sync/cleanup-executions, api/sync/cleanup-api-usage, api/sync/cleanup-search-logs, dashboard/admin, dashboard/admin/users).

9. Optional Next Step:
   The implementation is complete. No explicit next steps were requested. The user may want to commit these changes or test them locally. If they want to verify the admin dashboard works end-to-end, they'd need to promote a user to ADMIN with: `UPDATE users SET role = 'ADMIN' WHERE email = '...';`

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/ajaxdavis/.REDACTED.jsonl
Please continue the conversation from where we left off without asking the user any further questions. Continue with the last task that you were asked to work on.

---

<task-notification>
<task-id>b3365d0</task-id>
<output-file>/private/tmp/claude-501/-Users-ajaxdavis-repos-tpmjs-tpmjs/tasks/b3365d0.output</output-file>
<status>completed</status>
<summary>Background command "Type-check all packages" completed (exit code 0)</summary>
</task-notification>
Read the output file to retrieve the result: /private/tmp/claude-501/-Users-ajaxdavis-repos-tpmjs-tpmjs/tasks/b3365d0.output

---

do all the prod migrations commit everything and push and write a .md file contain everything i have to do, where i can find stuff etc and post it to discord